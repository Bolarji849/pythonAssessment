tuple1 = (1,2,3)
#withot a prentices it will still work prentices  is reduntant
singleton_tuple = 2,


tuple1 = list(tuple1)
#here we cast tuple back to list 
tuple1[1] = 10
# now tuple can now assign index 1 to 10 without error because cast

print(list("jesse")
# this inbuilt function help us turn string jesse into 

print(tuple1)
print(singleton_tuple)


list
tuple


board = [0] *9

for i in rang (1,len(board)):

	if board[i] % 3 == 0 break

print(board)
